/**
 * Sync unwrap helper that returns result tag and data.
 */
export { RuntimeUnwrapTaggedSync as Sync } from '../../operations/runtime-unwrap-tagged-sync'

/**
 * Async unwrap helper that returns result tag and data.
 */
export { RuntimeUnwrapTaggedAsync as Async } from '../../operations/runtime-unwrap-tagged-async'
