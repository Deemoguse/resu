/**
 * Sync unwrap helper that returns result data.
 */
export { RuntimeUnwrapSync as Sync } from '../../operations/runtime-unwrap-sync'

/**
 * Async unwrap helper that returns result data.
 */
export { RuntimeUnwrapAsync as Async } from '../../operations/runtime-unwrap-async'
