/**
 * Async function wrapper that resolves to flow results.
 */
export { FlowFunctionAsync as Async } from '../../operations/flow-function-async'

/**
 * Sync function wrapper that returns flow results.
 */
export { FlowFunctionSync as Sync } from '../../operations/flow-function-sync'
